{
  "name": "@video/edge",
  "private": true,
  "scripts": { "dev": "wrangler dev", "build": "wrangler deploy --dry-run", "deploy": "wrangler deploy", "lint": "tsc --noEmit", "typecheck": "tsc --noEmit", "test": "vitest run --passWithNoTests" },
  "devDependencies": { "@cloudflare/workers-types": "^5.20260804.1", "typescript": "^5.8.3", "vitest": "^3.2.4", "wrangler": "^4.118.0" }
}
